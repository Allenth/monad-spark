# MonadSpark 工作规则

先读 README.md、PROJECT_MEMORY.md、docs/SESSION_HANDOFF.md、docs/DECISIONS_AND_CONVERSATION.md 和 docs/projects/monad-spark/PROJECT_PLAN.md。

- 使用直白中文；把确认、建议、未知、已执行分开记录。
- 唯一项目经理为当前 Codex 主代理；项目所有者为本对话用户。计划当前 Draft，不代表已经批准开发。
- 本地仓库独立于外层 AutoWebFactory；不要把外层其他项目文件加入本仓库，也不要导入外层用户未提交改动。
- 此工作区继承外层项目治理：新设计/开发前需要批准计划、明确 production/experiment；网站工作还需选定生产流和单元验收。比赛允许原型不等于自动批准生产降级。
- 项目名称 monad-spark；monad-developers 是参考组织，不改名。
- 不打印、提交或复制 MOJO 凭证；只向 mojo.devnads.com 发送 Bearer。认领已完成，不重复注册。
- 提交前 GET 查询现有项目及比赛阶段；真实截图须上传和确认，禁止伪造回执、截图 ID、部署地址或完成状态。
- 用户有占位提交意图，但仍需有效材料及平台资格；不得把介绍页冒充已运行合约产品。
- 钱包确认不等于正式企业签约有效；用户文件、私钥、身份资料不放公开链或仓库。
- 使用脚手架前核实许可证、依赖与当前 Monad 网络规则。当前无安装/构建/测试命令。
- 每次实质变化更新项目记忆、执行记录和相关决定；没有证据不能写“已完成”。

## 新会话接续协议

此文件是项目 Agent 的持久入口；子 Agent 本身不保证跨会话存在。新会话必须打开本项目根目录或其工作树，才能使用这些文件。不要假定新会话自动继承聊天全文。

1. 读取上列入口；根据任务再读产品范围、技术研究或提交指南，不要求用户重复解释已记录信息。
2. 先运行 `pwd`、`git rev-parse --show-toplevel`、`git status --short --branch`、`git log -5 --oneline`，核实根目录、分支、未提交改动与交接基线。不要自动切分支、pull、reset、重新初始化、创建新仓库或重新注册 MOJO。
3. 对照交接记录检查实际文件；实际 Git/运行证据说明已做什么，最新用户明确决定说明要做什么。发现冲突先记录，不能静默把建议变成批准。
4. 用几句中文说明当前进度、承接任务及限制，然后推进已授权工作。只有缺少影响任务的关键决定才询问。
5. 二次开发优先在现有实现上做增量修改；先检查已有代码、接口、测试和约定。当前没有产品代码，文档准备不能当作已完成应用。
6. 新需求与旧范围相容时延续现有计划；明确改方向时保留历史并更新决定和计划，不擅自删除原内容。
7. 多会话同时开发时，先确认各自分支/工作树和文件归属；避免同时编辑同一文件。交接中记录实际任务与路径，不把未启动任务写成正在运行。
8. 结束或切换前更新 SESSION_HANDOFF、项目记忆、相关决定及执行日志：已完成、未完成、验证、下一步、阻塞、工作区状态。若无法更新，在最终回复中明确说明。
9. 有产品代码后记录真实可执行的启动/测试命令与结果；没有执行的命令标为未验证。平台状态、依赖和活动时间在实际使用时重新查询。
10. 不把授权扩大为自动公开发布、收费、真实签约或无限期后台运行；已确认的授权不重复询问。
